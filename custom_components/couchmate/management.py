"""CouchMate pairing, profile, and room-design management page."""
from __future__ import annotations

from aiohttp import web
from homeassistant.components.http import HomeAssistantView


class CouchMateManagementView(HomeAssistantView):
    """Serve a small authenticated-by-API management application."""

    url = "/couchmate/management"
    name = "couchmate:management"
    requires_auth = False

    async def get(self, request: web.Request) -> web.Response:
        return web.Response(
            text=HTML,
            content_type="text/html",
            headers={"Cache-Control": "no-store"},
        )


async def async_setup_management(hass) -> None:
    hass.http.register_view(CouchMateManagementView())


HTML = r'''<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CouchMate Core Dev Preview – Apple TVs & Design</title><style>
:root{color-scheme:dark;--bg:#0f1416;--surface:#171d20;--surface2:#20282c;--muted:#a7b1b6;--accent:#35C5B6;--accent-rgb:53,197,182;--line:#39464c;--danger:#ff9b9b;--ok:#79d8a8;--warning:#ffd88a}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:#f5f7f7;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.wrap{max-width:1480px;margin:auto;padding:38px 50px 86px}.top{display:flex;align-items:flex-start;justify-content:space-between;gap:28px}.eyebrow{color:var(--accent);font-weight:750;letter-spacing:.04em;text-transform:uppercase;font-size:13px}h1{font-size:40px;line-height:1.08;margin:8px 0 10px}h2{font-size:28px;margin:0 0 20px}h3{font-size:19px;margin:0}.sub{color:var(--muted);font-size:17px;line-height:1.45}.nav{display:flex;gap:8px;background:#101719;border:1px solid var(--line);border-radius:16px;padding:6px}.nav a{color:var(--muted);text-decoration:none;padding:11px 15px;border-radius:11px;font-weight:700}.nav a.active{background:var(--surface2);color:#fff}.section{margin-top:46px}.section-head{display:flex;align-items:end;justify-content:space-between;gap:20px;margin-bottom:18px}.panel,.card{background:var(--surface);border:1px solid var(--line);border-radius:24px}.panel{padding:24px}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(285px,1fr));gap:18px}.card{overflow:hidden}.card-body{padding:21px}.card-top{display:flex;justify-content:space-between;gap:15px;align-items:flex-start}.badge{display:inline-flex;align-items:center;gap:7px;margin-top:12px;padding:6px 10px;border-radius:999px;background:#101719;color:var(--accent);font-size:13px}.badge.area{background:#17312d}.badge.home{color:#d5e4e1}.badge.generated{color:var(--muted)}.row{display:flex;align-items:center;gap:12px;flex-wrap:wrap}.form{display:grid;grid-template-columns:minmax(220px,1fr) minmax(220px,1fr) auto;gap:12px}.field,select{width:100%;background:#101719;color:#fff;border:1px solid var(--line);border-radius:14px;padding:13px 14px;font:inherit}.button{border:0;border-radius:14px;padding:13px 17px;font:inherit;font-weight:750;cursor:pointer;background:var(--accent);color:#071411}.button.secondary{background:var(--surface2);color:#fff;border:1px solid var(--line)}.button.danger{background:transparent;color:var(--danger);border:1px solid #6b4448}.button:disabled{opacity:.48;cursor:not-allowed}.button:focus-visible,.nav a:focus-visible,.field:focus-visible,select:focus-visible,.file:focus-visible+.button{outline:3px solid var(--accent);outline-offset:3px}.client{display:grid;grid-template-columns:minmax(240px,1fr) minmax(240px,360px) auto;gap:18px;align-items:center;padding:18px 0;border-top:1px solid var(--line)}.client:first-child{border-top:0;padding-top:0}.client:last-child{padding-bottom:0}.pairing-heading{display:flex;align-items:center;gap:11px;flex-wrap:wrap}.count-badge{display:inline-grid;place-items:center;min-width:30px;height:30px;padding:0 9px;border-radius:999px;background:#173b35;color:var(--accent);font-size:14px;font-weight:800}.pairing-meta{min-height:22px;text-align:right;color:var(--muted);font-size:14px}.pairing-list{display:grid;gap:14px}.pairing-card{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:24px;align-items:center;border:1px solid var(--line);border-radius:19px;background:#13191c;padding:21px}.pairing-card[aria-busy="true"]{opacity:.7}.pairing-top{display:flex;align-items:flex-start;gap:14px}.pairing-icon{display:grid;place-items:center;flex:0 0 auto;width:46px;height:46px;border-radius:14px;background:#17312d;color:var(--accent);font-size:22px}.pairing-code-row{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-top:10px}.pairing-code{display:inline-block;color:#fff;background:#0b1012;border:1px solid #445259;border-radius:10px;padding:7px 10px;font:750 16px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.035em;white-space:nowrap}.pairing-time{color:var(--muted);font-size:14px;font-variant-numeric:tabular-nums}.permissions{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin-top:13px}.permission{display:inline-flex;align-items:center;border-radius:999px;background:#20292c;color:#dbe3e4;padding:6px 10px;font-size:13px}.permission.write{background:#3a3020;color:var(--warning)}.pairing-warning{margin-top:11px;color:#e8c77f;font-size:14px;line-height:1.4}.pairing-actions{display:flex;gap:9px;justify-content:flex-end;flex-wrap:wrap}.pairing-empty{display:grid;place-items:center;min-height:155px;text-align:center;color:var(--muted);padding:24px}.pairing-empty-icon{display:grid;place-items:center;width:46px;height:46px;margin-bottom:12px;border-radius:50%;background:#17312d;color:var(--ok);font-size:22px}.pairing-empty strong{display:block;color:#fff;font-size:18px;margin-bottom:5px}.preview{aspect-ratio:16/9;background:linear-gradient(135deg,#172d2a,#101719 60%,#253036);display:grid;place-items:center;overflow:hidden}.preview img{width:100%;height:100%;object-fit:cover}.preview span{color:var(--muted);font-size:14px;padding:18px;text-align:center}.preview.generated{background:radial-gradient(circle at 24% 18%,rgba(var(--accent-rgb),.23),transparent 31%),linear-gradient(145deg,#18312d,#101719 57%,#263239)}.actions{display:flex;gap:9px;margin-top:17px;flex-wrap:wrap}.file{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}.privacy{display:flex;gap:14px;align-items:flex-start;background:#10201e;border:1px solid #31534d;border-radius:18px;padding:18px 20px;color:#cce9e4}.privacy strong{display:block;color:#fff;margin-bottom:4px}.home-background{padding:0;display:grid;grid-template-columns:minmax(0,1.5fr) minmax(320px,.65fr);overflow:hidden}.home-preview{aspect-ratio:auto;min-height:360px;border-radius:23px 0 0 23px}.home-content{padding:30px;display:flex;flex-direction:column;justify-content:center}.home-content h3{font-size:25px;margin:8px 0 10px}.home-note{margin-top:14px;padding-top:14px;border-top:1px solid var(--line);color:var(--muted);font-size:14px;line-height:1.45}.room-head{display:flex;align-items:end;justify-content:space-between;gap:20px;margin:38px 0 18px}.room-head h3{font-size:23px;margin-bottom:5px}.toast{position:fixed;right:26px;bottom:26px;max-width:520px;border:1px solid var(--line);border-radius:18px;background:var(--surface2);padding:17px 20px;box-shadow:0 18px 54px rgba(0,0,0,.42);z-index:10}.toast.ok{border-color:var(--ok)}.toast.error{border-color:var(--danger)}.hidden{display:none!important}@media(max-width:900px){.home-background{grid-template-columns:1fr}.home-preview{min-height:260px;border-radius:23px 23px 0 0}.pairing-card{grid-template-columns:1fr}.pairing-actions{justify-content:flex-start}}@media(max-width:760px){.wrap{padding:26px 18px 70px}.top,.section-head,.room-head{display:block}.nav{margin-top:22px;overflow:auto}.form,.grid{grid-template-columns:minmax(0,1fr)}.client{grid-template-columns:1fr}.client>.button{width:100%}.panel{padding:18px}.home-background{padding:0}.home-content{padding:22px}.home-preview{min-height:220px}.pairing-meta{text-align:left;margin-top:9px}.pairing-card{padding:17px}.pairing-actions{display:grid;grid-template-columns:1fr 1fr}.pairing-actions .button{width:100%}.toast{left:18px;right:18px;bottom:18px}h1{font-size:32px}}
.pairing-list{grid-template-columns:minmax(0,1fr)}.pairing-card,.pairing-top,.pairing-top>div:last-child{min-width:0}.pairing-top h3{overflow-wrap:anywhere}.pairing-empty-icon{margin-inline:auto}.client-actions{display:flex;gap:9px;flex-wrap:wrap;justify-content:flex-end}
</style></head><body><main class="wrap"><header class="top"><div><div class="eyebrow">CouchMate Core Dev Preview</div><h1>Apple TVs & Design</h1><div class="sub">Gemeinsame Einstellungen bleiben lokal in deinem Home Assistant.</div></div><nav class="nav"><a href="/couchmate/configurator">Geräte & Funktionen</a><a class="active" href="/couchmate/management">Apple TVs & Design</a></nav></header>
<section class="section"><div class="privacy"><div>⌂</div><div><strong>Lokal gespeichert</strong>Profile und bearbeitete Raumbilder werden ausschließlich im CouchMate Core Dev Preview gespeichert. Originalbilder werden nicht aufbewahrt oder an einen Cloud-Dienst übertragen.</div></div></section>
<section class="section" aria-labelledby="pairingTitle"><div class="section-head"><div><div class="pairing-heading"><h2 id="pairingTitle">Offene Kopplungsanfragen</h2><span id="pairingCount" class="count-badge hidden" aria-live="polite">0</span></div><div class="sub">Neue Apple TVs und Companion Apps kannst du hier direkt zulassen oder ablehnen.</div></div><div><button id="refreshPairings" class="button secondary" type="button">Aktualisieren</button><div id="pairingMeta" class="pairing-meta" aria-live="polite"></div></div></div><div id="pairingPanel" class="panel"><div class="pairing-empty"><div><div class="pairing-empty-icon" aria-hidden="true">⌛</div><strong>Kopplungsanfragen werden geladen</strong><span>Einen Moment bitte.</span></div></div></div></section>
<section class="section"><div class="section-head"><div><h2>Profile</h2><div class="sub">Apple TVs und Companion Apps können Profile gemeinsam verwenden oder getrennt konfiguriert werden.</div></div></div><div class="panel"><form id="profileForm" class="form"><input id="profileName" class="field" maxlength="60" placeholder="Profilname, z. B. Wohnzimmer" required><select id="copyProfile"><option value="">Leeres Profil</option></select><button class="button" type="submit">Profil erstellen</button></form><div id="profileGrid" class="grid" style="margin-top:22px"></div></div></section>
<section class="section"><div class="section-head"><div><h2>Verbundene Apple TVs &amp; Companion Apps</h2><div class="sub">Jedes gekoppelte Gerät behält einen eigenen Zugangsschlüssel und erhält das hier zugewiesene Profil.</div></div></div><div id="clientPanel" class="panel"></div></section>
<section class="section"><div class="section-head"><div><h2>Hintergründe</h2><div class="sub">JPEG oder PNG ab 1280 × 720. Der Core entfernt Metadaten und speichert nur 16:9-Versionen.</div></div></div><article class="panel home-background" aria-labelledby="homeBackgroundTitle"><div id="homeBackgroundPreview" class="preview home-preview generated" role="img" aria-label="Vorschau des gemeinsamen Zuhause-Hintergrunds"><span>Generierter CouchMate-Hintergrund</span></div><div class="home-content"><div class="eyebrow">Ganzes Zuhause</div><h3 id="homeBackgroundTitle">Gemeinsamer Hintergrund</h3><div id="homeBackgroundStatus" class="sub">Wird geladen …</div><div class="actions"><input class="file" id="homeBackgroundFile" type="file" accept="image/jpeg,image/png" aria-label="Gemeinsamen Zuhause-Hintergrund auswählen"><label id="homeBackgroundLabel" class="button secondary" for="homeBackgroundFile">Bild auswählen</label><button id="removeHomeBackground" class="button danger" type="button" disabled>Zuhause-Hintergrund entfernen</button></div><div class="home-note">Dieser Hintergrund wird automatisch in allen Räumen ohne eigenes Raumbild verwendet. Eigene Raumbilder bleiben beim Ersetzen oder Entfernen unverändert.</div></div></article><div class="room-head"><div><h3>Raumhintergründe</h3><div class="sub">Ein eigenes Raumbild überschreibt den gemeinsamen Hintergrund nur in diesem Raum.</div></div></div><div id="backgroundGrid" class="grid"></div></section></main><div id="toast" class="toast hidden" role="status" aria-live="polite"></div><script>
let state=null,renderGeneration=0,pairingRequests=[],pairingLastUpdated=null,pairingTimer=null,pairingCountdownTimer=null,pairingLoading=false,pairingAuthDenied=false,pairingRendered=false;const objectURLs=new Map(),previewBlobs=new Map();
const auth=()=>{try{const t=JSON.parse(localStorage.getItem('hassTokens')||'{}');return t.access_token?{'Authorization':'Bearer '+t.access_token}:{} }catch(_){return {}}};
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function notify(kind,title,text){toast.className='toast '+kind;toast.innerHTML=`<strong>${esc(title)}</strong><div>${esc(text)}</div>`;clearTimeout(notify.timer);notify.timer=setTimeout(()=>toast.classList.add('hidden'),5500)}
async function api(url,options={}){options.headers={...(options.headers||{}),...auth()};const response=await fetch(url,options);let body={};if(response.status!==204){try{body=await response.json()}catch(_){body={}}}if(!response.ok){const message=response.status===401||response.status===403?'Nur Home-Assistant-Administratoren dürfen diese Seite verwenden.':body.message||body.error||`HTTP ${response.status}`;const error=new Error(message);error.status=response.status;error.code=body.error;throw error}return body}
function clearObjectURLs(){for(const url of objectURLs.values())URL.revokeObjectURL(url);objectURLs.clear()}
async function reload(){state=await api('/api/couchmate/v2/admin/configuration');renderGeneration+=1;previewBlobs.clear();clearObjectURLs();render()}
const pairingBase='/api/couchmate/v2/admin/pairing-requests';
const capabilityNames={'configuration:write':'Profile & Dashboard ändern','backgrounds:write':'Raumbilder verwalten','dashboard:write':'Dashboard-Kacheln anordnen'};
function normalizedCapabilities(item){return Array.isArray(item?.capabilities)?[...new Set(item.capabilities.filter(value=>typeof value==='string'))]:[]}
function clientKind(item){return normalizedCapabilities(item).some(value=>value==='configuration:write'||value==='backgrounds:write')?'Companion App':'Apple TV'}
function pairingSignature(items){return JSON.stringify(items.map(item=>[item.session_id,item.code,item.device_name,item.status,item.created_at,item.expires_at,normalizedCapabilities(item)]))}
function pairingExpiry(item){const remaining=Number(item.expires_in);if(Number.isFinite(remaining))return Date.now()+Math.max(0,remaining)*1000;const parsed=Date.parse(item.expires_at);return Number.isFinite(parsed)?parsed:Date.now()}
function requestedAt(value){const date=new Date(value);return Number.isFinite(date.getTime())?'Angefragt um '+new Intl.DateTimeFormat('de-DE',{hour:'2-digit',minute:'2-digit'}).format(date):'Neue Anfrage'}
function remainingText(seconds){seconds=Math.max(0,Math.ceil(seconds));if(seconds<=0)return'Abgelaufen';const minutes=Math.floor(seconds/60),rest=seconds%60;return`${minutes}:${String(rest).padStart(2,'0')} verbleibend`}
function pairingPermissionBadges(item){const capabilities=normalizedCapabilities(item),base='<span class="permission">Ausgewählte Geräte ansehen & steuern</span>';return base+capabilities.map(value=>`<span class="permission write">${esc(capabilityNames[value]||'Zusätzliches Recht')}</span>`).join('')}
function pairingHasWriteAccess(item){return normalizedCapabilities(item).length>0}
function pairingUpdatedLabel(){if(!navigator.onLine)return'Offline · Aktualisierung pausiert';if(!pairingLastUpdated)return'';return'Zuletzt geprüft: '+new Intl.DateTimeFormat('de-DE',{hour:'2-digit',minute:'2-digit'}).format(pairingLastUpdated)}
function updatePairingMeta(){pairingMeta.textContent=pairingUpdatedLabel()}
function updatePairingCountdowns(){if(document.hidden)return;document.querySelectorAll('[data-pairing-countdown]').forEach(element=>{const seconds=(Number(element.dataset.expiresAt)-Date.now())/1000;element.textContent=remainingText(seconds);const card=element.closest('[data-pairing-card]');if(seconds<=0&&card){card.querySelectorAll('button').forEach(button=>button.disabled=true)}})}
function synchronizePairingCountdowns(){pairingRequests.forEach(item=>{const card=[...pairingPanel.querySelectorAll('[data-pairing-card]')].find(element=>element.dataset.pairingCard===item.session_id),countdown=card?.querySelector('[data-pairing-countdown]');if(countdown)countdown.dataset.expiresAt=String(pairingExpiry(item))});updatePairingCountdowns()}
function pairingEmptyMarkup(icon,title,text){return`<div class="pairing-empty"><div><div class="pairing-empty-icon" aria-hidden="true">${icon}</div><strong>${esc(title)}</strong><span>${esc(text)}</span></div></div>`}
function renderPairings(error=null){const count=pairingRequests.length;if(pairingCount.textContent!==String(count))pairingCount.textContent=String(count);pairingCount.classList.toggle('hidden',count===0);if(error){pairingPanel.innerHTML=pairingEmptyMarkup(error.status===401||error.status===403?'⌾':'! ',error.status===401||error.status===403?'Administratorzugriff erforderlich':'Kopplungsanfragen nicht erreichbar',error.message);pairingRendered=true;return}if(!count){pairingPanel.innerHTML=pairingEmptyMarkup('✓','Keine offenen Kopplungsanfragen','Starte die Verbindung auf dem Apple TV. Neue Anfragen erscheinen hier automatisch.');pairingRendered=true;return}pairingPanel.innerHTML=`<div class="pairing-list">${pairingRequests.map(item=>{const capabilities=normalizedCapabilities(item),hasWrite=capabilities.length>0,expiry=pairingExpiry(item);return`<article class="pairing-card" data-pairing-card="${esc(item.session_id)}"><div class="pairing-top"><div class="pairing-icon" aria-hidden="true">▣</div><div><h3>${esc(item.device_name||'Apple TV')}</h3><div class="pairing-code-row"><code class="pairing-code">${esc(item.code)}</code><span class="pairing-time">${esc(requestedAt(item.created_at))}</span><span class="pairing-time" data-pairing-countdown data-expires-at="${expiry}" aria-live="off">${remainingText((expiry-Date.now())/1000)}</span></div><div class="permissions">${pairingPermissionBadges(item)}</div>${hasWrite?'<div class="pairing-warning">Dieser Apple TV fordert zusätzliche Änderungsrechte an.</div>':''}</div></div><div class="pairing-actions"><button class="button" type="button" data-pairing-approve="${esc(item.session_id)}">Zulassen</button><button class="button danger" type="button" data-pairing-reject="${esc(item.session_id)}">Ablehnen</button></div></article>`}).join('')}</div>`;pairingPanel.querySelectorAll('[data-pairing-approve]').forEach(button=>button.onclick=()=>actOnPairing(button.dataset.pairingApprove,'approve'));pairingPanel.querySelectorAll('[data-pairing-reject]').forEach(button=>button.onclick=()=>actOnPairing(button.dataset.pairingReject,'reject'));pairingRendered=true;updatePairingCountdowns()}
function schedulePairingRefresh(delay=null){clearTimeout(pairingTimer);pairingTimer=null;if(document.hidden||!navigator.onLine||pairingAuthDenied)return;pairingTimer=setTimeout(()=>loadPairingRequests(),delay??(pairingRequests.length?10000:30000))}
async function loadPairingRequests({manual=false}={}){if(pairingLoading)return;pairingLoading=true;clearTimeout(pairingTimer);pairingTimer=null;if(manual){refreshPairings.disabled=true;refreshPairings.textContent='Aktualisieren …'}try{const previous=pairingSignature(pairingRequests),payload=await api(pairingBase,{cache:'no-store'}),next=Array.isArray(payload.pairing_requests)?payload.pairing_requests.filter(item=>item&&item.status==='waiting'):[];pairingRequests=next;pairingLastUpdated=new Date();pairingAuthDenied=false;if(!pairingRendered||previous!==pairingSignature(next))renderPairings();else synchronizePairingCountdowns();updatePairingMeta()}catch(error){if(error.status===401||error.status===403){pairingRequests=[];pairingAuthDenied=true}if(!pairingRendered||pairingAuthDenied)renderPairings(error);updatePairingMeta();if(manual&&!pairingAuthDenied)notify('error','Aktualisierung fehlgeschlagen',error.message)}finally{pairingLoading=false;if(manual){refreshPairings.disabled=false;refreshPairings.textContent='Aktualisieren'}schedulePairingRefresh()}}
async function actOnPairing(sessionID,action){const item=pairingRequests.find(request=>request.session_id===sessionID);if(!item)return;if(action==='approve'&&pairingHasWriteAccess(item)){const rights=normalizedCapabilities(item).map(value=>capabilityNames[value]||'Zusätzliches Recht').join(', ');if(!confirm(`„${item.device_name||'Apple TV'}“ zulassen? Zusätzlich angeforderte Rechte: ${rights}.`))return}clearTimeout(pairingTimer);pairingTimer=null;const card=[...pairingPanel.querySelectorAll('[data-pairing-card]')].find(element=>element.dataset.pairingCard===sessionID);if(card){card.setAttribute('aria-busy','true');card.querySelectorAll('button').forEach(button=>button.disabled=true)}try{await api(`${pairingBase}/${encodeURIComponent(sessionID)}/${action}`,{method:'POST'});pairingRequests=pairingRequests.filter(request=>request.session_id!==sessionID);renderPairings();pairingLastUpdated=new Date();updatePairingMeta();schedulePairingRefresh();notify('ok',action==='approve'?'Apple TV zugelassen':'Kopplung abgelehnt',item.device_name||'Apple TV')}catch(error){if(error.status===404||error.status===409){await loadPairingRequests();notify('','Anfrage nicht mehr verfügbar','Die Liste wurde aktualisiert.')}else{if(card){card.removeAttribute('aria-busy');card.querySelectorAll('button').forEach(button=>button.disabled=false)}schedulePairingRefresh();notify('error',action==='approve'?'Zulassen fehlgeschlagen':'Ablehnen fehlgeschlagen',error.message)}}}
function profileOptions(selected){return state.profiles.map(p=>`<option value="${esc(p.id)}" ${p.id===selected?'selected':''}>${esc(p.name)}</option>`).join('')}
function backgroundSource(area){return ['area','home','generated'].includes(area.background_source)?area.background_source:'generated'}
function sourceLabel(source){return source==='area'?'Eigenes Raumbild':source==='home'?'Zuhause-Hintergrund':'Generierter Hintergrund'}
function render(){
copyProfile.innerHTML='<option value="">Leeres Profil</option>'+state.profiles.map(p=>`<option value="${esc(p.id)}">Kopie von ${esc(p.name)}</option>`).join('');
profileGrid.innerHTML=state.profiles.map(p=>`<article class="card"><div class="card-body"><div class="card-top"><div><h3>${esc(p.name)}</h3><div class="sub">Revision ${p.revision}</div></div>${p.id==='default'?'<span class="badge">Standard</span>':''}</div><div class="actions"><button class="button secondary" type="button" data-rename="${esc(p.id)}">Umbenennen</button><button class="button danger" type="button" data-delete="${esc(p.id)}" ${p.id==='default'?'disabled':''}>Löschen</button></div></div></article>`).join('');
profileGrid.querySelectorAll('[data-rename]').forEach(b=>b.onclick=()=>renameProfile(b.dataset.rename));profileGrid.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>deleteProfile(b.dataset.delete));
clientPanel.innerHTML=state.clients.length?state.clients.map(c=>{const kind=clientKind(c),name=c.device_name||kind;return`<div class="client"><div><h3>${esc(name)}</h3><div class="sub">${esc(kind)} · Zuletzt verbunden: ${esc(c.last_seen||'Noch nicht')}</div></div><select aria-label="Profil für ${esc(name)}" data-client-profile="${esc(c.client_id)}">${profileOptions(c.profile_id)}</select><div class="client-actions"><button class="button secondary" type="button" data-client-rename="${esc(c.client_id)}">Umbenennen</button><button class="button danger" type="button" data-client-delete="${esc(c.client_id)}" aria-label="${esc(kind)} ${esc(name)} löschen">Löschen</button></div></div>`}).join(''):'<div class="sub">Noch kein Apple TV und keine Companion App gekoppelt.</div>';
clientPanel.querySelectorAll('[data-client-profile]').forEach(s=>s.onchange=()=>assignProfile(s.dataset.clientProfile,s.value));clientPanel.querySelectorAll('[data-client-rename]').forEach(b=>b.onclick=()=>renameClient(b.dataset.clientRename));clientPanel.querySelectorAll('[data-client-delete]').forEach(b=>b.onclick=()=>deleteClient(b.dataset.clientDelete));
const home=state.home_background||null,homeRevision=home?.revision!=null?` · Revision ${home.revision}`:'';homeBackgroundStatus.textContent=home?`Aktiv für alle Räume ohne eigenes Bild${homeRevision}`:'Kein eigenes Bild · CouchMate verwendet den generierten Hintergrund.';homeBackgroundLabel.textContent=home?'Bild ersetzen':'Bild auswählen';removeHomeBackground.disabled=!home;homeBackgroundPreview.classList.toggle('generated',!home);homeBackgroundPreview.innerHTML=`<span>${home?'Vorschau wird geladen …':'Generierter CouchMate-Hintergrund'}</span>`;
if(home)loadPreview(home,'homeBackgroundPreview','global','Zuhause-Hintergrund');
backgroundGrid.innerHTML=state.areas.map(a=>{const source=backgroundSource(a),hasOverride=source==='area',revision=a.background?.revision;const detail=source==='area'?`Nur für diesen Raum${revision?' · Revision '+esc(revision):''}`:source==='home'?`Vom Zuhause-Hintergrund übernommen${revision?' · Revision '+esc(revision):''}`:'Kein eigenes Bild gespeichert';return `<article class="card"><div class="preview ${source==='generated'?'generated':''}" id="preview-${esc(a.id)}" role="img" aria-label="Hintergrundvorschau für ${esc(a.name)}"><span>${a.background?'Vorschau wird geladen …':'Generierter CouchMate-Hintergrund'}</span></div><div class="card-body"><div class="card-top"><div><h3>${esc(a.name)}</h3><span class="badge ${source}">${sourceLabel(source)}</span></div></div><div class="sub" style="margin-top:11px">${detail}</div><div class="actions"><input class="file" id="file-${esc(a.id)}" data-file="${esc(a.id)}" type="file" accept="image/jpeg,image/png" aria-label="Eigenes Hintergrundbild für ${esc(a.name)} auswählen"><label class="button secondary" for="file-${esc(a.id)}">${hasOverride?'Bild ersetzen':'Eigenes Bild wählen'}</label><button class="button danger" type="button" data-remove-bg="${esc(a.id)}" ${hasOverride?'':'disabled'} title="${hasOverride?'Eigenes Raumbild entfernen und den verfügbaren Hintergrund übernehmen':'Für diesen Raum ist kein eigenes Bild gespeichert'}">Raumbild entfernen</button></div></div></article>`}).join('');
backgroundGrid.querySelectorAll('[data-file]').forEach(input=>input.onchange=()=>uploadBackground(input.dataset.file,input.files?.[0]));backgroundGrid.querySelectorAll('[data-remove-bg]').forEach(button=>button.onclick=()=>removeBackground(button.dataset.removeBg));for(const area of state.areas){if(area.background){const descriptor=backgroundSource(area)==='home'&&state.home_background?state.home_background:area.background;loadPreview(descriptor,'preview-'+area.id,'area:'+area.id,area.name)}}
}
profileForm.onsubmit=async event=>{event.preventDefault();const name=profileName.value.trim();if(!name)return;try{await api('/api/couchmate/v2/admin/profiles',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,copy_from_profile_id:copyProfile.value||null,expected_revision:state.revision})});profileName.value='';await reload();notify('ok','Profil erstellt',name)}catch(error){notify('error','Profil konnte nicht erstellt werden',error.message)}};
async function renameProfile(id){const profile=state.profiles.find(item=>item.id===id);const name=prompt('Neuer Profilname',profile?.name||'');if(!name?.trim())return;try{await api('/api/couchmate/v2/admin/profiles/'+encodeURIComponent(id),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:name.trim(),expected_revision:state.revision})});await reload();notify('ok','Profil umbenannt',name.trim())}catch(error){notify('error','Änderung fehlgeschlagen',error.message)}}
async function deleteProfile(id){const profile=state.profiles.find(item=>item.id===id);if(!profile||!confirm(`Profil „${profile.name}“ löschen? Zugewiesene Geräte wechseln zum Standardprofil.`))return;try{await api('/api/couchmate/v2/admin/profiles/'+encodeURIComponent(id),{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({expected_revision:state.revision})});await reload();notify('ok','Profil gelöscht',profile.name)}catch(error){notify('error','Profil konnte nicht gelöscht werden',error.message)}}
async function assignProfile(clientID,profileID){try{await api('/api/couchmate/v2/admin/clients/'+encodeURIComponent(clientID)+'/profile',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({profile_id:profileID,expected_revision:state.revision})});await reload();notify('ok','Profil zugewiesen','Die Änderung wird beim nächsten Abgleich übernommen.')}catch(error){await reload();notify('error','Zuweisung fehlgeschlagen',error.message)}}
async function renameClient(clientID){const client=state.clients.find(item=>item.client_id===clientID);if(!client)return;const name=prompt('Neuer Gerätename',client.device_name||clientKind(client));if(!name?.trim())return;try{await api('/api/couchmate/v2/admin/clients/'+encodeURIComponent(clientID),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({device_name:name.trim()})});await reload();notify('ok','Gerät umbenannt',name.trim())}catch(error){notify('error','Gerät konnte nicht umbenannt werden',error.message)}}
async function deleteClient(clientID){const client=state.clients.find(item=>item.client_id===clientID);if(!client)return;const kind=clientKind(client),name=client.device_name||kind;if(!confirm(`${kind} „${name}“ löschen? Die Kopplung und der gespeicherte Zugangsschlüssel werden entfernt. Danach muss das Gerät erneut gekoppelt werden.`))return;const button=[...clientPanel.querySelectorAll('[data-client-delete]')].find(item=>item.dataset.clientDelete===clientID),row=button?.closest('.client');row?.querySelectorAll('button,select').forEach(control=>control.disabled=true);try{await api('/api/couchmate/v2/admin/clients/'+encodeURIComponent(clientID),{method:'DELETE'});await reload();notify('ok',`${kind} gelöscht`,`${name} ist nicht mehr gekoppelt.`)}catch(error){await reload();notify('error',`${kind} konnte nicht gelöscht werden`,error.message)}}
async function decodedImage(file){if(typeof createImageBitmap==='function'){try{const bitmap=await createImageBitmap(file,{imageOrientation:'from-image'});return{source:bitmap,width:bitmap.width,height:bitmap.height,close:()=>bitmap.close()}}catch(_){}}const url=URL.createObjectURL(file);return await new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve({source:image,width:image.naturalWidth,height:image.naturalHeight,close:()=>URL.revokeObjectURL(url)});image.onerror=()=>{URL.revokeObjectURL(url);reject(new Error('Das Bild konnte nicht gelesen werden.'))};image.src=url})}
async function processedJPEG(file){if(!file||(file.type&&!['image/jpeg','image/png'].includes(file.type)))throw new Error('Bitte ein JPEG- oder PNG-Bild auswählen.');if(file.size>10*1024*1024)throw new Error('Das Bild darf höchstens 10 MiB groß sein.');const decoded=await decodedImage(file);try{if(decoded.width>8192||decoded.height>8192||decoded.width*decoded.height>32000000)throw new Error('Das Bild ist zu groß. Maximal erlaubt sind 32 Megapixel und 8192 Pixel pro Kante.');const sourceRatio=decoded.width/decoded.height;const targetRatio=16/9;let sx=0,sy=0,sw=decoded.width,sh=decoded.height;if(sourceRatio>targetRatio){sw=Math.floor(decoded.height*targetRatio);sx=Math.floor((decoded.width-sw)/2)}else{sh=Math.floor(decoded.width/targetRatio);sy=Math.floor((decoded.height-sh)/2)}if(sw<1280||sh<720)throw new Error('Das Bild ist für einen TV-Hintergrund zu klein.');const width=Math.min(3840,sw);const height=Math.round(width/targetRatio);const canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const context=canvas.getContext('2d',{alpha:false});if(!context)throw new Error('Das Bild kann in diesem Browser nicht verarbeitet werden.');context.drawImage(decoded.source,sx,sy,sw,sh,0,0,width,height);const blob=await new Promise((resolve,reject)=>canvas.toBlob(value=>value?resolve(value):reject(new Error('Das Bild konnte nicht verarbeitet werden.')),'image/jpeg',.86));if(blob.size>10*1024*1024)throw new Error('Das verarbeitete Bild ist noch zu groß. Bitte wähle ein weniger detailreiches Bild.');return blob}finally{decoded.close()}}
async function uploadHomeBackground(file){if(!file)return;try{notify('','Zuhause-Hintergrund wird vorbereitet','Das Original bleibt unverändert.');const blob=await processedJPEG(file);await api('/api/couchmate/v2/admin/home-background',{method:'PUT',headers:{'Content-Type':'image/jpeg'},body:blob});await reload();notify('ok','Zuhause-Hintergrund gespeichert','Alle Räume ohne eigenes Bild übernehmen ihn automatisch.')}catch(error){notify('error','Zuhause-Hintergrund konnte nicht gespeichert werden',error.message)}finally{homeBackgroundFile.value=''}}
async function removeGlobalBackground(){if(!state.home_background||!confirm('Gemeinsamen Zuhause-Hintergrund entfernen? Eigene Raumbilder bleiben erhalten; alle anderen Räume wechseln zum generierten CouchMate-Hintergrund.'))return;try{await api('/api/couchmate/v2/admin/home-background',{method:'DELETE'});await reload();notify('ok','Zuhause-Hintergrund entfernt','Räume ohne eigenes Bild verwenden jetzt den generierten CouchMate-Hintergrund.')}catch(error){notify('error','Zuhause-Hintergrund konnte nicht entfernt werden',error.message)}}
async function uploadBackground(areaID,file){if(!file)return;try{notify('','Bild wird vorbereitet','Das Original bleibt unverändert.');const blob=await processedJPEG(file);await api('/api/couchmate/v2/admin/backgrounds/'+encodeURIComponent(areaID),{method:'PUT',headers:{'Content-Type':'image/jpeg'},body:blob});await reload();notify('ok','Raumbild gespeichert','Der Core hat ausschließlich bereinigte 16:9-Versionen gespeichert.')}catch(error){notify('error','Raumbild konnte nicht gespeichert werden',error.message)}finally{const input=document.getElementById('file-'+areaID);if(input)input.value=''}}
async function removeBackground(areaID){const area=state.areas.find(item=>item.id===areaID);if(!area||backgroundSource(area)!=='area')return;const fallback=state.home_background?'den gemeinsamen Zuhause-Hintergrund':'den generierten CouchMate-Hintergrund';if(!confirm(`Eigenes Raumbild für „${area.name}“ entfernen? Danach verwendet der Raum ${fallback}.`))return;try{await api('/api/couchmate/v2/admin/backgrounds/'+encodeURIComponent(areaID),{method:'DELETE'});await reload();notify('ok','Eigenes Raumbild entfernt',`${area.name} verwendet jetzt ${fallback}.`)}catch(error){notify('error','Raumbild konnte nicht entfernt werden',error.message)}}
async function previewBlob(url){if(!previewBlobs.has(url))previewBlobs.set(url,(async()=>{const response=await fetch(url,{headers:auth()});if(response.status===401||response.status===403)throw new Error('AUTH');if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.blob()})());return previewBlobs.get(url)}
async function loadPreview(background,previewID,cacheKey,label){const generation=renderGeneration,preview=document.getElementById(previewID),url=background?.admin_preview_url;if(!preview||!url)return;try{const blob=await previewBlob(url);if(generation!==renderGeneration)return;const previous=objectURLs.get(cacheKey);if(previous)URL.revokeObjectURL(previous);const objectURL=URL.createObjectURL(blob);objectURLs.set(cacheKey,objectURL);preview.classList.remove('generated');preview.innerHTML=`<img alt="" aria-hidden="true" src="${objectURL}">`}catch(_){if(generation!==renderGeneration)return;preview.innerHTML=`<span>Vorschau für ${esc(label)} nicht verfügbar</span>`}}
homeBackgroundFile.onchange=()=>uploadHomeBackground(homeBackgroundFile.files?.[0]);removeHomeBackground.onclick=removeGlobalBackground;refreshPairings.onclick=()=>loadPairingRequests({manual:true});document.addEventListener('visibilitychange',()=>{if(document.hidden){clearTimeout(pairingTimer);pairingTimer=null}else loadPairingRequests()});window.addEventListener('online',()=>{updatePairingMeta();loadPairingRequests()});window.addEventListener('offline',()=>{clearTimeout(pairingTimer);pairingTimer=null;updatePairingMeta()});window.addEventListener('beforeunload',()=>{clearTimeout(pairingTimer);clearInterval(pairingCountdownTimer);clearObjectURLs()});pairingCountdownTimer=setInterval(updatePairingCountdowns,1000);
loadPairingRequests();reload().catch(error=>notify('error','Verwaltung konnte nicht geladen werden',error.message));
</script></body></html>'''
